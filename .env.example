# Modèle de configuration — copier vers .env et remplir les vraies valeurs :
#   cp backend/.env.example backend/.env

# ── Base de données PostgreSQL ───────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sig_eau_bf
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe

# ── Télédétection Copernicus / Sentinel Hub ──────────────────────────────────
SH_CLIENT_ID=votre_client_id
SH_CLIENT_SECRET=votre_client_secret
